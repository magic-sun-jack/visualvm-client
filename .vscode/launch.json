{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Run VisualVM Client Dev",
      "command": "pnpm dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
