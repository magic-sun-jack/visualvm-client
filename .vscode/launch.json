{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Run Local",
      "command": "pnpm dev-local",
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Run Deploy",
      "command": "pnpm deploy",
      "cwd": "${workspaceFolder}"
    }
  ]
}
